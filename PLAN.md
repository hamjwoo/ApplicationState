# ApplicationState

**Capstone 트랙**: 트랙 A — 풀스택 웹앱
**구현에 쓸 수 있는 시간**: 코칭 전후·저녁 + 다음 날 발표 전까지 (실질 6~8시간)

## ① 문제 정의 — 지금 무엇이 불편한가
- 누가 / 언제 / 어떤 일에서: 본인이 하루 2번, 서버 PC의 필수 애플리케이션 상태(state_sample.json 형태 로그)를 확인할 때
- 줄이는 비용 (반복 / 대기 / 검증 중 하나): 반복 — 매번 로그 파일을 열어 실행 중인지, 재시작이 성공했는지 손으로 확인해야 함
- 지금 걸리는 시간·횟수: 하루 2번, 확인 자체에 오래 걸리지는 않음

## ② AI 에이전트에게 맡길 부분
- 에이전트가 대신할 일: 코드 작성 전부 (API, 화면, 10초 주기 갱신 로직, 이력 기능, 테스트용 이력 로그 샘플 생성)
- 사람이 계속 할 일: 서버 PC에 배포
- 쓸 도구 (서브에이전트·MCP·스킬·hook·병렬 중 필요한 것만): 백로그 + hook 사용 예정 (구체적으로 어디에 왜 쓸지는 미정 — 1:1 코칭에서 확정, 추후 스펙 작성 시 정함)

## ③ 범위 — 반드시 / 되면 좋은 / 안 하는 것
- 반드시 (이게 안 되면 실패): 로그(state_sample.json)를 읽어 화면에 로그와 동일한 정보(앱별 상태·재시작 결과)가 나온다 + 10초마다 주기적으로 로그를 재탐색해 화면 상태를 업데이트한다
- 되면 좋은: 애플리케이션의 동작/종료 이력 확인 (이력 로그 샘플이 없어 직접 생성해서 테스트해야 함)
- 이번엔 안 하는 것: 웹페이지 로그인(인증)

## ④ Phase 분할과 각 Phase 완료 기준
1. 데이터 읽기 API — state_sample.json을 파싱해 반환하는 API 구축  — 완료: API를 호출하면 로그와 동일한 내용이 반환된다
2. 화면 표시 — API를 불러와 앱 목록(상태 포함)을 보여주는 페이지  — 완료: 페이지를 열면 로그와 동일한 앱 개수·상태가 화면에 나온다
3. 주기적 갱신 — 10초마다 로그를 재탐색해 화면 상태를 갱신  — 완료: 로그 파일을 바꾸면 10초 후 화면도 동일하게 바뀐다
4. 이력 기능 (되면 좋은 것) — 상태 변화 이력을 기록·표시, 이력 로그 샘플 직접 생성해 테스트  — 완료: 상태가 바뀔 때마다 이력이 추가된다
5. 마무리 — 전체 연결 확인  — 완료: 명령 한 번으로 서버가 뜨고 화면이 에러 없이 열린다

## ⑤ 완료를 판정할 방법 (검증 게이트)
- 기계가 판정하는 것: ④의 각 Phase "완료:" 조건 (API 응답 일치, 화면 표시 일치, 로그 변경 반영, 이력 추가, 서버 기동+화면 로드)
- 사람이 눈으로 보는 것: 화면 디자인 (테스트 PC/웹페이지라 크게 볼 것 없음)
- 내가 직접 승인할 지점: 주기적 갱신 로직(10초 주기 재탐색)을 실행하기 전

## GitHub 저장소 URL

## 1:1 코칭에서 가장 묻고 싶은 것
없음

---
(아래는 폼에 넣지 않는 작업 메모)

## 되돌아갈 지점과 데이터
- 되돌리는 방법: git 저장소로 초기화, 시작 커밋 d6e4cd6 (state_sample.json, PLAN.md) — 문제 생기면 이 커밋으로 복귀
- 실데이터·자격증명 없이 되게 만드는 방법 (샘플로 대체할 부분): state_sample.json이 이미 익명 샘플(app-1~app-10) — 그대로 사용

## 고칠 곳 (4단계 반박 결과)
1. (④ Phase 3) [해결됨] 주기를 5분 → 10초로 바꿔서 검증 대기 시간 문제 해소
2. (② AI 역할 — 쓸 도구) 백로그+hook 사용은 구체적 용도가 아직 미정 — 스펙 나오기 전엔 계약에 실체 없음
3. (⑤ 승인 지점) 주기적 갱신 실행 전 승인은 있음, 배포는 이미 사람 몫으로 분리돼 있어 추가로 빠진 곳은 안 보임

<!-- 기획 시작 15:23 -->
