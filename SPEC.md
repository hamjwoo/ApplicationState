# SPEC.md — ApplicationState

## 목적
서버 PC에서 반드시 실행되고 있어야 하는 애플리케이션들의 상태를 로그(state.json)로 기록하고 있지만, 지금은 그 로그를 사람이 직접 열어 눈으로 확인해야 한다. 이 문서는 그 로그를 읽어 로컬 웹페이지에 상태를 보여주고, 10초마다 자동으로 로그를 재탐색해 화면을 최신 상태로 유지하는 대시보드의 구현 스펙을 정의한다.

## 화면 흐름
1. 브라우저에서 페이지(`/`)를 열면 서버가 `data/state.json`을 읽어 각 애플리케이션의 이름·상태·재시작 결과를 목록으로 보여준다.
2. 화면은 새로고침 없이 10초마다 자동으로 `/api/status`를 다시 호출해 목록을 갱신한다.
3. 화면 안에 "이력" 영역이 있어 `/api/history`를 호출해, 지금까지 상태가 바뀐 기록을 시간 순으로 보여준다.
4. 클릭이나 입력으로 조작하는 기능은 없다 — 읽기 전용 대시보드다.

## 데이터 구조

### 상태 항목 — `data/state.json` (배열)
- `id`: string (UUID) — 앱 고유 식별자
- `name`: string — 앱 이름
- `status`: `"running"` | `"closed"`
- `exitType`: string, optional — `status`가 `"closed"`일 때만 존재 (예: `"normal"`)
- `restartOutcome`: `"success"` | `"fail"`, optional — 없으면 화면에 "재시작 이력 없음(정상)"으로 표시

### 이력 항목 — `data/history.json` (배열)
- `id`: string — 대상 앱의 id
- `name`: string — 대상 앱 이름
- `fromStatus`: string — 변경 전 상태
- `toStatus`: string — 변경 후 상태
- `timestamp`: string (ISO 8601) — 변경이 감지된 시각

## 제약
- 실데이터·사내 자격증명 사용하지 않는다 — `state_sample.json` 형태의 샘플만 사용한다.
- 로그인/인증 기능은 만들지 않는다 (로컬 사용자 전용).
- 로그 재탐색 주기는 기본값 10초이며, 환경변수(`RESCAN_INTERVAL_MS`)로 변경할 수 있다.
- 이력은 파일(JSON)에 저장한다 — 별도 데이터베이스는 쓰지 않는다.
- 백엔드는 Node.js + Express, 프론트엔드는 순수 HTML/CSS/JS를 쓴다 (프레임워크·빌드 도구 없음).
- 재탐색 스케줄러는 실제로 켜기 전에 사용자 승인을 받는다 (주기 값과 무관).

## 완료 조건 (브라우저에서 확인 가능한 문장)
1. 브라우저에서 페이지를 열면 `data/state.json`에 있는 앱 개수와 동일한 개수의 항목이 화면에 나타난다.
2. 화면에 보이는 각 항목의 상태(running/closed)와 재시작 결과 표시가 `data/state.json`의 값과 일치한다.
3. `data/state.json`의 값을 수정하고 10초를 기다리면, 새로고침 없이 화면의 값이 수정된 값으로 바뀐다.
4. 상태가 바뀐 뒤 이력 영역을 보면, 방금 바뀐 변경 건이 새 항목으로 추가되어 있다.
5. `npm start`를 한 번 실행하면 서버가 뜨고, 브라우저에서 에러 화면 없이 위 1~4 항목이 모두 확인된다.
